export const W = 960;
export const H = 520;

// Altura total do mundo (muito maior que a tela)
export const WORLD_H   = 20000;

// Superfície do chão (Y dos pés, bem abaixo)
export const GROUND_Y  = 7400;

// Nuvem inicial (perto do topo)
export const CLOUD_X   = W / 2;
export const CLOUD_Y   = 2400;  // Y da superfície da nuvem (500m do solo)
export const CLOUD_W   = 280;
export const CLOUD_H   = 60;

// Mesa de ferramentas — superfície, lado esquerdo
export const WORKBENCH_X = 120;

// Tamanho de cada célula de terra escavável (px)
export const CELL_SIZE = 40;
